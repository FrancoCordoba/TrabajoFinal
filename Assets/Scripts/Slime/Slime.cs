using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Slime : MonoBehaviour
{

   /* [SerializeField] private GameObject originPointOne;
    [SerializeField] private float distanceRay = 10f;
    public float speedToLoock = 0f;
    public float speedEnemy = 10f;
   

    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        FindEnemy();
        
    }
    public void FindEnemy()
    {
        BroadcastRaycast(originPointOne.transform);
    }
    private void BroadcastRaycast(Transform origen)
    {
     
    }
    public void DrawRaycast(Transform origen)
    {
        Gizmos.color = Color.cyan;
        Vector3 direction = origen.TransformDirection(Vector3.forward) * distanceRay;
        Gizmos.DrawRay(origen.position, direction);
    }
    private void OnDrawGizmos()
    {
        DrawRaycast(originPointOne.transform);
    }*/
}
